#!/bin/sh

echo ">° >< Losing memmory at $1 MB/s..."

block_size_in_mb=$(($1*1024*1024))

while true
do
	allocated="${allocated}`printf %${block_size_in_mb}s`"
	i=$((i + $1))
	echo $i MB
	sleep 1
done
